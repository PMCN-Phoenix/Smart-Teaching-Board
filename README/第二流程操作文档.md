✋ 第二阶段：轮廓初现 —— 业务闭环与交互层（增加“人”的维度）

从“正方体”到“人形石头”，我们需要把这堆技术跑通成一个真正的**教学系统**。  
下面我把整个第二阶段拆成 **6 个任务包（6.1 ~ 6.6）**，每个任务包依然保持  
**“完整代码 → 拆解讲解 → 验收标准 → Git 备份”** 的铁律。

---

## 📦 任务包 6.1：用户体系与 JWT 鉴权（预计 60 分钟）

**目标**：在后端建立 `User` 表，实现注册/登录接口，返回 JWT，所有后续接口都要验证身份。

**涉及文件**：
- `backend/pom.xml`（添加依赖）
- `backend/src/main/resources/application.yml`（JWT 配置）
- `backend/src/main/java/com/yourcompany/board/entity/User.java`
- `backend/src/main/java/com/yourcompany/board/repository/UserRepository.java`
- `backend/src/main/java/com/yourcompany/board/dto/LoginRequest.java`  
- `backend/src/main/java/com/yourcompany/board/dto/RegisterRequest.java`  
- `backend/src/main/java/com/yourcompany/board/dto/AuthResponse.java`  
- `backend/src/main/java/com/yourcompany/board/utils/JwtUtil.java`
- `backend/src/main/java/com/yourcompany/board/config/JwtAuthenticationFilter.java`
- `backend/src/main/java/com/yourcompany/board/config/SecurityConfig.java`
- `backend/src/main/java/com/yourcompany/board/controller/AuthController.java`

**数据库**：需要 MySQL，后续任务会建表。

---

### ☑️ 1. 添加依赖（pom.xml）

在 `backend/pom.xml` 的 `<dependencies>` 内追加：

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security</artifactId>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-api</artifactId>
    <version>0.11.5</version>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-impl</artifactId>
    <version>0.11.5</version>
    <scope>runtime</scope>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-jackson</artifactId>
    <version>0.11.5</version>
    <scope>runtime</scope>
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-validation</artifactId>
</dependency>
```

> **要点**：验证是否下载成功，可执行 `mvn clean compile`。

---

### ☑️ 2. 配置 JWT（application.yml）

在 `application.yml` 中追加：

```yaml
jwt:
  secret: Yn2k3j4h5g6f7d8s9a0p1o2i3u4y5t6r7e8w9q0  # 请换成32位以上随机字符串
  expiration: 86400000  # 24小时

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_board?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: your_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
```

> **要点**：确保 MySQL 中已经创建了 `smart_board` 库。`ddl-auto: update` 会自动建表。

---

### ☑️ 3. 创建 User 实体与 Repository

`backend/src/main/java/com/yourcompany/board/entity/User.java`  
（完整代码，直接复制）

```java
package com.yourcompany.board.entity;

import javax.persistence.*;
import java.time.LocalDateTime;

@Entity
@Table(name = "users")
public class User {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(unique = true, nullable = false, length = 50)
    private String username;

    @Column(nullable = false)
    private String password;  // BCrypt 加密后的密码

    @Column(unique = true, length = 100)
    private String email;

    private LocalDateTime createdAt;

    public User() {}

    public User(String username, String password, String email) {
        this.username = username;
        this.password = password;
        this.email = email;
        this.createdAt = LocalDateTime.now();
    }

    // getters and setters ...
    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }
    public String getUsername() { return username; }
    public void setUsername(String username) { this.username = username; }
    public String getPassword() { return password; }
    public void setPassword(String password) { this.password = password; }
    public String getEmail() { return email; }
    public void setEmail(String email) { this.email = email; }
    public LocalDateTime getCreatedAt() { return createdAt; }
    public void setCreatedAt(LocalDateTime createdAt) { this.createdAt = createdAt; }
}
```

`UserRepository.java`：

```java
package com.yourcompany.board.repository;

import com.yourcompany.board.entity.User;
import org.springframework.data.jpa.repository.JpaRepository;
import java.util.Optional;

public interface UserRepository extends JpaRepository<User, Long> {
    Optional<User> findByUsername(String username);
    Boolean existsByUsername(String username);
}
```

---

### ☑️ 4. DTO 类

`LoginRequest.java`：
```java
package com.yourcompany.board.dto;

import javax.validation.constraints.NotBlank;

public class LoginRequest {
    @NotBlank
    private String username;
    @NotBlank
    private String password;
    // getters/setters ...
}
```

`RegisterRequest.java`：
```java
package com.yourcompany.board.dto;

import javax.validation.constraints.*;

public class RegisterRequest {
    @NotBlank @Size(min=3, max=50)
    private String username;
    @NotBlank @Size(min=6, max=100)
    private String password;
    @Email
    private String email;
    // getters/setters ...
}
```

`AuthResponse.java`：
```java
package com.yourcompany.board.dto;

public class AuthResponse {
    private String token;
    private String username;
    public AuthResponse(String token, String username) {
        this.token = token;
        this.username = username;
    }
    // getters
}
```

---

### ☑️ 5. JwtUtil 工具类

`backend/src/main/java/com/yourcompany/board/utils/JwtUtil.java`

```java
package com.yourcompany.board.utils;

import io.jsonwebtoken.*;
import io.jsonwebtoken.security.Keys;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

import java.security.Key;
import java.util.Date;

@Component
public class JwtUtil {
    private final Key key;
    private final long expiration;

    public JwtUtil(@Value("${jwt.secret}") String secret,
                   @Value("${jwt.expiration}") long expiration) {
        this.key = Keys.hmacShaKeyFor(secret.getBytes());
        this.expiration = expiration;
    }

    public String generateToken(String username) {
        Date now = new Date();
        Date expiry = new Date(now.getTime() + expiration);
        return Jwts.builder()
                .setSubject(username)
                .setIssuedAt(now)
                .setExpiration(expiry)
                .signWith(key, SignatureAlgorithm.HS256)
                .compact();
    }

    public String getUsernameFromToken(String token) {
        return Jwts.parserBuilder().setSigningKey(key).build()
                .parseClaimsJws(token).getBody().getSubject();
    }

    public boolean validateToken(String token) {
        try {
            Jwts.parserBuilder().setSigningKey(key).build().parseClaimsJws(token);
            return true;
        } catch (JwtException e) {
            return false;
        }
    }
}
```

---

### ☑️ 6. JWT 过滤器与 SecurityConfig

`JwtAuthenticationFilter.java`：
```java
package com.yourcompany.board.config;

import com.yourcompany.board.utils.JwtUtil;
import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;
import org.springframework.security.core.context.SecurityContextHolder;
import org.springframework.web.filter.OncePerRequestFilter;

import javax.servlet.FilterChain;
import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.io.IOException;
import java.util.Collections;

public class JwtAuthenticationFilter extends OncePerRequestFilter {
    private final JwtUtil jwtUtil;

    public JwtAuthenticationFilter(JwtUtil jwtUtil) {
        this.jwtUtil = jwtUtil;
    }

    @Override
    protected void doFilterInternal(HttpServletRequest request,
                                    HttpServletResponse response,
                                    FilterChain chain) throws ServletException, IOException {
        String header = request.getHeader("Authorization");
        if (header != null && header.startsWith("Bearer ")) {
            String token = header.substring(7);
            if (jwtUtil.validateToken(token)) {
                String username = jwtUtil.getUsernameFromToken(token);
                UsernamePasswordAuthenticationToken auth =
                        new UsernamePasswordAuthenticationToken(username, null, Collections.emptyList());
                SecurityContextHolder.getContext().setAuthentication(auth);
            }
        }
        chain.doFilter(request, response);
    }
}
```

`SecurityConfig.java`：
```java
package com.yourcompany.board.config;

import com.yourcompany.board.utils.JwtUtil;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.http.SessionCreationPolicy;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.security.web.SecurityFilterChain;
import org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter;

@Configuration
public class SecurityConfig {
    private final JwtUtil jwtUtil;

    public SecurityConfig(JwtUtil jwtUtil) {
        this.jwtUtil = jwtUtil;
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http.csrf().disable()
            .sessionManagement().sessionCreationPolicy(SessionCreationPolicy.STATELESS)
            .and()
            .authorizeRequests()
            .antMatchers("/api/auth/**").permitAll()   // 注册登录放行
            .anyRequest().authenticated()
            .and()
            .addFilterBefore(new JwtAuthenticationFilter(jwtUtil),
                             UsernamePasswordAuthenticationFilter.class);
        return http.build();
    }
}
```

---

### ☑️ 7. AuthController（注册/登录）

```java
package com.yourcompany.board.controller;

import com.yourcompany.board.dto.*;
import com.yourcompany.board.entity.User;
import com.yourcompany.board.repository.UserRepository;
import com.yourcompany.board.utils.JwtUtil;
import org.springframework.http.ResponseEntity;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.web.bind.annotation.*;

import javax.validation.Valid;

@RestController
@RequestMapping("/api/auth")
public class AuthController {
    private final UserRepository userRepository;
    private final PasswordEncoder passwordEncoder;
    private final JwtUtil jwtUtil;

    public AuthController(UserRepository userRepository,
                          PasswordEncoder passwordEncoder,
                          JwtUtil jwtUtil) {
        this.userRepository = userRepository;
        this.passwordEncoder = passwordEncoder;
        this.jwtUtil = jwtUtil;
    }

    @PostMapping("/register")
    public ResponseEntity<?> register(@Valid @RequestBody RegisterRequest req) {
        if (userRepository.existsByUsername(req.getUsername())) {
            return ResponseEntity.badRequest().body("用户名已存在");
        }
        User user = new User(
            req.getUsername(),
            passwordEncoder.encode(req.getPassword()),
            req.getEmail()
        );
        userRepository.save(user);
        String token = jwtUtil.generateToken(user.getUsername());
        return ResponseEntity.ok(new AuthResponse(token, user.getUsername()));
    }

    @PostMapping("/login")
    public ResponseEntity<?> login(@Valid @RequestBody LoginRequest req) {
        User user = userRepository.findByUsername(req.getUsername())
                .orElse(null);
        if (user == null || !passwordEncoder.matches(req.getPassword(), user.getPassword())) {
            return ResponseEntity.status(401).body("用户名或密码错误");
        }
        String token = jwtUtil.generateToken(user.getUsername());
        return ResponseEntity.ok(new AuthResponse(token, user.getUsername()));
    }
}
```

---

### ☑️ 8. 验证与 Git 备份

**启动顺序**：
1. 确保 MySQL 运行，库已建。
2. `cd backend && mvn spring-boot:run`

**测试注册**：
```bash
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"123456","email":"a@b.com"}'
```
返回 `{"token":"eyJ...","username":"test"}`。

**测试登录**：
```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"123456"}'
```

**测试受保护资源**（后续会用到，现在会 403 或 500 无接口）：
```bash
curl http://localhost:8080/api/something -H "Authorization: Bearer <token>"
```

> ✅ **验收标准**：注册返回 token，登录返回 token，无 token 访问其他接口返回 401/403。

> 💾 **Git 备份**：
```bash
git add backend/pom.xml backend/src/main/resources/application.yml
git add backend/src/main/java/com/yourcompany/board/entity/User.java
git add backend/src/main/java/com/yourcompany/board/repository/UserRepository.java
git add backend/src/main/java/com/yourcompany/board/dto/
git add backend/src/main/java/com/yourcompany/board/utils/JwtUtil.java
git add backend/src/main/java/com/yourcompany/board/config/JwtAuthenticationFilter.java
git add backend/src/main/java/com/yourcompany/board/config/SecurityConfig.java
git add backend/src/main/java/com/yourcompany/board/controller/AuthController.java
git commit -m "feat: 用户体系、注册登录、JWT鉴权"
git push
```

---

## 📦 任务包 6.2：课堂与对话数据模型 + API（预计 45 分钟）

**目标**：建立 Classroom、AnalysisRecord、ConversationMessage 三张表，以及对应的 CRUD 接口。后续所有业务都挂在这些表上。

**文件列表**：
- entity: `Classroom.java`, `AnalysisRecord.java`, `ConversationMessage.java`
- repository: 对应三个 Repository
- dto: `ClassroomCreateRequest.java`, `ClassroomResponse.java`, `AnalysisResponse.java`(稍微扩展)
- controller: `ClassroomController.java`
- 修改 AI 转发控制器，融入课堂上下文（下一任务包做）。

---

### ☑️ 1. 实体类

`Classroom.java`：
```java
package com.yourcompany.board.entity;

import javax.persistence.*;
import java.time.LocalDateTime;
import java.util.ArrayList;
import java.util.List;

@Entity
@Table(name = "classrooms")
public class Classroom {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(nullable = false)
    private String title;

    private String description;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "user_id")
    private User user;

    private LocalDateTime createdAt = LocalDateTime.now();
    private LocalDateTime updatedAt = LocalDateTime.now();

    @OneToMany(mappedBy = "classroom", cascade = CascadeType.ALL, orphanRemoval = true)
    private List<AnalysisRecord> records = new ArrayList<>();

    @OneToMany(mappedBy = "classroom", cascade = CascadeType.ALL, orphanRemoval = true)
    private List<ConversationMessage> messages = new ArrayList<>();

    // constructors, getters, setters ...
    public Classroom() {}
    public Classroom(String title, String description, User user) {
        this.title = title;
        this.description = description;
        this.user = user;
    }
    // 请补全 getters/setters（字段较多，此处省略，请根据实际补全）
}
```

`AnalysisRecord.java`：
```java
package com.yourcompany.board.entity;

import javax.persistence.*;
import java.time.LocalDateTime;

@Entity
@Table(name = "analysis_records")
public class AnalysisRecord {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "classroom_id")
    private Classroom classroom;

    @Lob
    private String imageBase64;  // 或存储文件路径，此处保留Base64便于数据库存储

    private String whiteboardType;

    @Lob
    private String aiResult;     // AI 返回的 Markdown 内容

    private LocalDateTime createdAt = LocalDateTime.now();

    // getters, setters...
}
```

`ConversationMessage.java`：
```java
package com.yourcompany.board.entity;

import javax.persistence.*;
import java.time.LocalDateTime;

@Entity
@Table(name = "conversation_messages")
public class ConversationMessage {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "classroom_id")
    private Classroom classroom;

    private String role;   // "user" 或 "assistant"

    @Lob
    private String content;

    private LocalDateTime timestamp = LocalDateTime.now();

    // getters, setters...
}
```

---

### ☑️ 2. Repository

`ClassroomRepository.java`：
```java
public interface ClassroomRepository extends JpaRepository<Classroom, Long> {
    List<Classroom> findByUserIdOrderByUpdatedAtDesc(Long userId);
}
```
其他两个 Repository 类似，暂略（可根据需要提供基本 CRUD）。

---

### ☑️ 3. DTO

`ClassroomCreateRequest.java`：
```java
public class ClassroomCreateRequest {
    @NotBlank
    private String title;
    private String description;
    // getters/setters
}
```

`ClassroomResponse.java`：
```java
public class ClassroomResponse {
    private Long id;
    private String title;
    private String description;
    private LocalDateTime createdAt;
    // constructor, getters
}
```

---

### ☑️ 4. ClassroomController（受保护资源）

```java
@RestController
@RequestMapping("/api/classrooms")
public class ClassroomController {
    private final ClassroomRepository classroomRepo;
    private final UserRepository userRepo;

    // constructor ...

    @PostMapping
    public ResponseEntity<?> create(@Valid @RequestBody ClassroomCreateRequest req,
                                    Principal principal) {
        User user = userRepo.findByUsername(principal.getName()).orElseThrow();
        Classroom c = new Classroom(req.getTitle(), req.getDescription(), user);
        classroomRepo.save(c);
        return ResponseEntity.ok(new ClassroomResponse(c.getId(), c.getTitle(), c.getDescription(), c.getCreatedAt()));
    }

    @GetMapping
    public List<ClassroomResponse> list(Principal principal) {
        User user = userRepo.findByUsername(principal.getName()).orElseThrow();
        return classroomRepo.findByUserIdOrderByUpdatedAtDesc(user.getId())
                .stream().map(c -> new ClassroomResponse(c.getId(), c.getTitle(), c.getDescription(), c.getCreatedAt()))
                .collect(Collectors.toList());
    }
}
```
> **要点**：`Principal principal` 由 Spring Security 注入，里面是用户名。

---

### ☑️ 5. 验证

启动后端，用注册的 token 测试：
```bash
curl -X POST http://localhost:8080/api/classrooms \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"title":"高等数学-第一课","description":"导数与微分"}'
```
应返回 JSON 包含 id 等信息。

> ✅ **验收标准**：能创建课堂，GET 能列出当前用户的课堂列表。

> 💾 **Git 备份**：
```bash
git add backend/src/main/java/com/yourcompany/board/entity/Classroom.java ...
git commit -m "feat: 课堂、分析记录、对话消息实体与API"
git push
```

---

## 📦 任务包 6.3：前端 Vue 项目重构与路由（预计 60 分钟）

**目标**：将之前的最小 Demo 升华为 Vue SPA，加入 Vue Router、登录页、课堂列表页、画板页、历史详情页的骨架。

我们假设现有 `frontend/src` 是 Vue 项目（查看 package.json 确认是否有 vue）。若不是，需先执行 `npm create vue@latest`。这里以已有的 Vite + Vue 项目为基础。

---

### ☑️ 1. 安装必要依赖
```bash
cd frontend
npm install vue-router@4 pinia axios marked highlight.js katex
```

---

### ☑️ 2. 目录结构规划
```
frontend/src/
  views/
    Login.vue
    Register.vue
    Dashboard.vue      (课堂列表)
    Board.vue          (主画板+对话)
    HistoryDetail.vue  (历史查看)
  components/
    ConversationPanel.vue
    AICard.vue
    AppToolbar.vue
  stores/
    auth.js
    classroom.js
  router/
    index.js
  services/
    api.js
  App.vue
  main.js
```

---

### ☑️ 3. 路由配置

`frontend/src/router/index.js`：
```javascript
import { createRouter, createWebHistory } from 'vue-router'
import Login from '../views/Login.vue'
import Register from '../views/Register.vue'
import Dashboard from '../views/Dashboard.vue'
import Board from '../views/Board.vue'
import HistoryDetail from '../views/HistoryDetail.vue'

const routes = [
  { path: '/', redirect: '/login' },
  { path: '/login', component: Login },
  { path: '/register', component: Register },
  { path: '/dashboard', component: Dashboard, meta: { requiresAuth: true } },
  { path: '/board/:classroomId', component: Board, meta: { requiresAuth: true } },
  { path: '/history/:classroomId', component: HistoryDetail, meta: { requiresAuth: true } },
]

const router = createRouter({
  history: createWebHistory(),
  routes,
})

// 路由守卫
import { useAuthStore } from '../stores/auth'
router.beforeEach((to, from, next) => {
  if (to.meta.requiresAuth) {
    const auth = useAuthStore()
    if (!auth.token) {
      next('/login')
    } else {
      next()
    }
  } else {
    next()
  }
})

export default router
```

---

### ☑️ 4. Pinia 状态管理

`auth.js`：
```javascript
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { api } from '../services/api'

export const useAuthStore = defineStore('auth', () => {
  const token = ref(localStorage.getItem('token') || '')
  const username = ref(localStorage.getItem('username') || '')

  function setAuth(t, u) {
    token.value = t
    username.value = u
    localStorage.setItem('token', t)
    localStorage.setItem('username', u)
  }

  function logout() {
    token.value = ''
    username.value = ''
    localStorage.clear()
  }

  async function login(credentials) {
    const res = await api.post('/api/auth/login', credentials)
    setAuth(res.data.token, res.data.username)
    return res.data
  }

  async function register(info) {
    const res = await api.post('/api/auth/register', info)
    setAuth(res.data.token, res.data.username)
    return res.data
  }

  return { token, username, login, register, logout }
})
```

`classroom.js`：
```javascript
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { api } from '../services/api'

export const useClassroomStore = defineStore('classroom', () => {
  const classrooms = ref([])
  const currentClassroom = ref(null)

  async function fetchClassrooms() {
    const res = await api.get('/api/classrooms')
    classrooms.value = res.data
  }

  async function createClassroom(title, description) {
    const res = await api.post('/api/classrooms', { title, description })
    await fetchClassrooms()
    return res.data
  }

  function setCurrent(c) { currentClassroom.value = c }

  return { classrooms, currentClassroom, fetchClassrooms, createClassroom, setCurrent }
})
```

---

### ☑️ 5. API 服务封装

`frontend/src/services/api.js`：
```javascript
import axios from 'axios'
import { useAuthStore } from '../stores/auth'

const api = axios.create({
  baseURL: '',  // 代理会处理
})

api.interceptors.request.use(config => {
  const auth = useAuthStore()
  if (auth.token) {
    config.headers.Authorization = `Bearer ${auth.token}`
  }
  return config
})

export { api }
```

---

### ☑️ 6. 登录/注册页面骨架

`Login.vue`（简化版）：
```vue
<template>
  <div>
    <h1>登录</h1>
    <input v-model="username" placeholder="用户名" />
    <input v-model="password" type="password" placeholder="密码" />
    <button @click="handleLogin">登录</button>
    <router-link to="/register">去注册</router-link>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import { useAuthStore } from '../stores/auth'
import { useRouter } from 'vue-router'

const auth = useAuthStore()
const router = useRouter()
const username = ref('')
const password = ref('')

async function handleLogin() {
  try {
    await auth.login({ username: username.value, password: password.value })
    router.push('/dashboard')
  } catch (e) {
    alert('登录失败')
  }
}
</script>
```

`Register.vue` 类似，略。

---

### ☑️ 7. Vite 代理配置确认

`vite.config.ts`：
```ts
export default defineConfig({
  server: {
    proxy: {
      '/api': 'http://localhost:8080'
    }
  }
})
```

---

### ☑️ 8. 验证

启动前端：`npm run dev`，访问 `http://localhost:5173`，应重定向到 `/login`，可注册、登录，然后跳转到 `/dashboard`（页面尚无，可先空白）。

> ✅ **验收标准**：登录后 token 存入 localStorage，路由守卫生效。

> 💾 **Git 备份**：
```bash
git add frontend/src/router frontend/src/stores frontend/src/services frontend/src/views/Login.vue frontend/src/views/Register.vue frontend/src/App.vue frontend/src/main.js
git commit -m "feat: 前端Vue重构，路由、状态管理、登录注册页"
git push
```

---

## 📦 任务包 6.4：画板与对话核心交互（预计 90 分钟）

**目标**：实现 `Board.vue`（进入课堂后的主界面），集成：
- 摄像头 + MediaPipe 手势写字（复用上一阶段代码，封装为组件）
- 圈选发送 AI（携带 classroomId）
- 对话式展示：用户追问，AI 基于历史回答，后端维护多轮上下文。
- 自动保存分析记录到数据库。

**关键变化**：
- 后端 `/api/classrooms/{id}/analyze` 接收图片后，初始化对话上下文（只包含当前分析的 prompt 和 AI 回答），保存记录和两条消息。
- 追问接口 `/api/classrooms/{id}/conversation`，传入新的用户消息，后端取出该课堂的所有消息，拼接成完整 messages 数组发送给 AI 微服务，返回后保存 assistant 消息。

AI 微服务需增加 `/conversation` 端点，支持传入完整 messages 数组。

---

### ☑️ 1. AI 微服务增加多轮对话支持

在 `ai-service/server.py` 中添加：

```python
from pydantic import BaseModel
from typing import List, Dict

class ConversationMessage(BaseModel):
    role: str
    content: str

class ConversationRequest(BaseModel):
    messages: List[ConversationMessage]
    context: Optional[str] = ""

@app.post("/conversation")
async def conversation(request: ConversationRequest):
    try:
        # 直接使用 messages 调用智谱
        payload = {
            "model": analyzer.model,
            "messages": [msg.dict() for msg in request.messages],
            "temperature": 0.1,
            "max_tokens": 2048
        }
        success, content, error = analyzer._call_api_with_retry(payload)
        return {"success": success, "content": content, "error_msg": error}
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))
```

> **要点**：无需修改 `ZhipuVisionAnalyzer`，因为 `_call_api_with_retry` 接收任意 payload。

---

### ☑️ 2. 后端中继接口改造

**修改 `AiController`**，添加两个端点。

`AiController.java`（完整版）：

```java
@RestController
@RequestMapping("/api")
public class AiController {

    @Value("${ai.service.url}")
    private String aiServiceUrl;

    private final RestTemplate restTemplate = new RestTemplate();
    private final ClassroomRepository classroomRepo;
    private final AnalysisRecordRepository recordRepo;
    private final ConversationMessageRepository msgRepo;
    private final UserRepository userRepo;

    // constructor...

    // 原有 /analyze 保留，但要增加课堂ID参数？
    // 改为：POST /api/classrooms/{classroomId}/analyze
    @PostMapping("/classrooms/{classroomId}/analyze")
    public ResponseEntity<?> analyze(@PathVariable Long classroomId,
                                     @RequestBody AnalyzeRequest request,
                                     Principal principal) {
        User user = userRepo.findByUsername(principal.getName()).orElseThrow();
        Classroom classroom = classroomRepo.findById(classroomId)
                .orElseThrow(() -> new RuntimeException("课堂不存在"));
        if (!classroom.getUser().getId().equals(user.getId())) {
            return ResponseEntity.status(403).body("无权操作");
        }
        // 转发给 AI
        HttpHeaders headers = new HttpHeaders();
        headers.setContentType(MediaType.APPLICATION_JSON);
        Map<String, Object> aiPayload = new HashMap<>();
        aiPayload.put("image_base64", request.getImageBase64());
        aiPayload.put("whiteboard_type", request.getWhiteboardType());
        aiPayload.put("context", request.getContext());
        HttpEntity<Map<String, Object>> entity = new HttpEntity<>(aiPayload, headers);

        ResponseEntity<AnalyzeResponse> aiResp = restTemplate.exchange(
                aiServiceUrl, HttpMethod.POST, entity, AnalyzeResponse.class);

        AnalyzeResponse body = aiResp.getBody();
        if (body != null && body.isSuccess()) {
            // 保存分析记录
            AnalysisRecord record = new AnalysisRecord();
            record.setClassroom(classroom);
            record.setImageBase64(request.getImageBase64());
            record.setWhiteboardType(request.getWhiteboardType());
            record.setAiResult(body.getContent());
            recordRepo.save(record);

            // 初始化对话消息：用户（图片描述）？我们存一条系统提示和 AI 回复
            ConversationMessage userMsg = new ConversationMessage();
            userMsg.setClassroom(classroom);
            userMsg.setRole("user");
            userMsg.setContent("[发送了白板截图]"); // 可替换为实际 prompt 描述
            msgRepo.save(userMsg);

            ConversationMessage assistantMsg = new ConversationMessage();
            assistantMsg.setClassroom(classroom);
            assistantMsg.setRole("assistant");
            assistantMsg.setContent(body.getContent());
            msgRepo.save(assistantMsg);

            return ResponseEntity.ok(body);
        } else {
            return ResponseEntity.status(500).body(body != null ? body : "AI 分析失败");
        }
    }

    // 追问端点
    @PostMapping("/classrooms/{classroomId}/conversation")
    public ResponseEntity<?> conversation(@PathVariable Long classroomId,
                                          @RequestBody Map<String, String> payload,
                                          Principal principal) {
        String userMessage = payload.get("message");
        // 权限校验
        Classroom classroom = classroomRepo.findById(classroomId).orElseThrow();
        if (!classroom.getUser().getUsername().equals(principal.getName())) {
            return ResponseEntity.status(403).body("无权访问");
        }
        // 构建消息列表
        List<ConversationMessage> existingMessages = msgRepo.findByClassroomOrderByTimestampAsc(classroomId);
        List<Map<String, String>> messages = new ArrayList<>();
        // 加上系统提示（可选）
        Map<String, String> systemMsg = new HashMap<>();
        systemMsg.put("role", "system");
        systemMsg.put("content", "你是一个大学教授，正在课堂中回答学生的追问。请基于之前的对话回答。");
        messages.add(systemMsg);
        for (ConversationMessage msg : existingMessages) {
            Map<String, String> m = new HashMap<>();
            m.put("role", msg.getRole());
            m.put("content", msg.getContent());
            messages.add(m);
        }
        // 加上当前用户消息
        Map<String, String> newUserMsg = new HashMap<>();
        newUserMsg.put("role", "user");
        newUserMsg.put("content", userMessage);
        messages.add(newUserMsg);

        // 保存用户消息
        ConversationMessage userMsgEntity = new ConversationMessage();
        userMsgEntity.setClassroom(classroom);
        userMsgEntity.setRole("user");
        userMsgEntity.setContent(userMessage);
        msgRepo.save(userMsgEntity);

        // 调用 AI 微服务 /conversation
        HttpHeaders headers = new HttpHeaders();
        headers.setContentType(MediaType.APPLICATION_JSON);
        Map<String, Object> aiRequest = new HashMap<>();
        aiRequest.put("messages", messages);
        HttpEntity<Map<String, Object>> entity = new HttpEntity<>(aiRequest, headers);
        ResponseEntity<Map> resp = restTemplate.exchange(
                aiServiceUrl.replace("/analyze", "/conversation"),
                HttpMethod.POST, entity, Map.class);

        if (resp.getStatusCode() == HttpStatus.OK && (boolean) resp.getBody().get("success")) {
            String aiContent = (String) resp.getBody().get("content");
            // 保存助手消息
            ConversationMessage assistantMsgEntity = new ConversationMessage();
            assistantMsgEntity.setClassroom(classroom);
            assistantMsgEntity.setRole("assistant");
            assistantMsgEntity.setContent(aiContent);
            msgRepo.save(assistantMsgEntity);
            return ResponseEntity.ok(Collections.singletonMap("content", aiContent));
        } else {
            return ResponseEntity.status(500).body("AI 对话失败");
        }
    }
}
```

> **注意**：需添加 `AnalysisRecordRepository`, `ConversationMessageRepository`。

---

### ☑️ 3. 前端 Board.vue 实现

`Board.vue` 组件需包含摄像头画布和右侧对话面板。由于代码量较大，这里给出关键结构，实际可复制改造 minimal-demo 并嵌入 Vue。

`Board.vue` 骨架：

```vue
<template>
  <div class="board-container">
    <div class="canvas-area">
      <video ref="video" autoplay playsinline style="transform: scaleX(-1);"></video>
      <canvas ref="outputCanvas"></canvas>
      <div class="toolbar">
        <button @click="clearCanvas">清屏</button>
        <input type="color" v-model="color" />
        <button @click="sendSelection" :disabled="!selection">发送AI</button>
      </div>
    </div>
    <div class="conversation-panel">
      <ConversationPanel :messages="messages" @send="handleFollowUp" />
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted, nextTick } from 'vue'
import { useRoute } from 'vue-router'
import { api } from '../services/api'
import ConversationPanel from '../components/ConversationPanel.vue'
// 引入 MediaPipe 等逻辑（封装成 composable 或在此实现）
// ...
const route = useRoute()
const classroomId = route.params.classroomId
const messages = ref([])
const selection = ref(null)

// 初始化手势写字逻辑（可复用 minimal-demo 代码）
// 发送圈选区域
async function sendSelection() {
  // 将 selection 区域截取为 base64
  const base64 = ... // canvas.toDataURL 去掉前缀
  const res = await api.post(`/api/classrooms/${classroomId}/analyze`, {
    imageBase64: base64,
    whiteboardType: 'general',
    context: ''
  })
  if (res.data.success) {
    // 添加 AI 消息到对话
    messages.value.push({ role: 'assistant', content: res.data.content })
  }
}

async function handleFollowUp(text) {
  // 添加用户消息
  messages.value.push({ role: 'user', content: text })
  const res = await api.post(`/api/classrooms/${classroomId}/conversation`, { message: text })
  messages.value.push({ role: 'assistant', content: res.data.content })
}
</script>
```

> **实际组合**：将 MediaPipe 初始化、手势写字、框选逻辑封装到组件或 composable 中，确保与 Vue 生命周期兼容。

---

### ☑️ 4. ConversationPanel 组件

```vue
<template>
  <div class="chat">
    <div v-for="(msg, idx) in messages" :key="idx" :class="msg.role">
      <div v-if="msg.role==='assistant'" v-html="renderMarkdown(msg.content)"></div>
      <div v-else>{{ msg.content }}</div>
    </div>
    <input v-model="inputText" @keyup.enter="send" placeholder="追问..." />
    <button @click="send">发送</button>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import { marked } from 'marked'
import hljs from 'highlight.js'
import katex from 'katex'

const props = defineProps(['messages'])
const emit = defineEmits(['send'])
const inputText = ref('')

function renderMarkdown(text) {
  // 简单集成高亮和公式，可用 marked 插件
  return marked.parse(text)
}

function send() {
  if (inputText.value.trim()) {
    emit('send', inputText.value)
    inputText.value = ''
  }
}
</script>
```

---

### ☑️ 5. 验证全流程

1. 启动所有服务（AI微服务、后端、前端）。
2. 注册登录，创建课堂，进入 Board 页面。
3. 允许摄像头，捏合写字，框选发送，观察右侧出现 AI Markdown 回答。
4. 在下方输入追问，观察多轮对话。

> ✅ **验收标准**：能完成“发送截图→AI识别→追问→AI再回答”的闭环，所有记录保存在数据库。

> 💾 **Git 备份**：
```bash
git add ai-service/server.py
git add backend/src/main/java/com/yourcompany/board/controller/AiController.java
# ... 添加新增实体、repository、前端页面等
git commit -m "feat: 对话式交互与课堂闭环"
git push
```

---

## 📦 任务包 6.5：内容模块分流与 UI 卡片（预计 45 分钟）

**目标**：根据 whiteboardType 渲染不同样式的 AI 回答卡片：
- 数学/物理：LaTeX 公式渲染（KaTeX），居中公式块。
- 代码：代码块带复制按钮，语法高亮。
- 通用：普通 Markdown 渲染。

在 `AICard.vue` 组件中实现。

---

### ☑️ 1. 创建 AICard 组件

`AICard.vue`：
```vue
<template>
  <div class="ai-card" :class="type">
    <div class="card-header">
      <span class="badge">{{ typeLabel }}</span>
      <button class="copy-btn" v-if="hasCode" @click="copyCode">复制代码</button>
    </div>
    <div class="card-body" ref="body" v-html="renderedContent"></div>
  </div>
</template>

<script setup>
import { computed, ref, onMounted, nextTick } from 'vue'
import { marked } from 'marked'
import hljs from 'highlight.js'
import katex from 'katex'

const props = defineProps({
  content: String,
  type: { type: String, default: 'general' }
})

const typeLabel = computed(() => ({
  math: '数学', physics: '物理', code: '代码', general: '通用'
}[props.type] || '通用'))

const hasCode = computed(() => props.type === 'code' || /```/.test(props.content))

function renderWithKatex(text) {
  // 简单替换 $$...$$ 和 $...$ 为 KaTeX 渲染后的 HTML
  return text.replace(/\$\$([^$]+)\$\$/g, (_, formula) => {
    try { return katex.renderToString(formula, { displayMode: true }) } catch { return _ }
  }).replace(/\$([^$]+)\$/g, (_, formula) => {
    try { return katex.renderToString(formula, { displayMode: false }) } catch { return _ }
  })
}

const renderedContent = computed(() => {
  let html = marked.parse(props.content)
  if (props.type === 'math' || props.type === 'physics') {
    html = renderWithKatex(html)
  }
  return html
})

function copyCode() {
  const code = props.content.match(/```(?:\w+\n)?([\s\S]*?)```/)?.[1] || ''
  navigator.clipboard.writeText(code)
  alert('代码已复制')
}

onMounted(async () => {
  await nextTick()
  // 高亮所有代码块
  document.querySelectorAll('.ai-card pre code').forEach(block => hljs.highlightElement(block))
})
</script>
```

> **要点**：在 ConversationPanel 中根据分析类型传递 `type`，可从分析记录中获取。

---

### ☑️ 2. 修改 ConversationPanel 使用 AICard

```vue
<AICard v-if="msg.role==='assistant'" :content="msg.content" :type="msg.type || 'general'" />
```

> **注意**：后端在返回分析结果时，需要把 `whiteboardType` 一同带回，前端存储到消息对象中。

---

### ☑️ 3. 验证

- 创建数学课堂，发送手写公式，返回的卡片应渲染 LaTeX。
- 代码类型应有复制按钮，点击复制。

> ✅ **验收标准**：不同类型的回答卡片有显著 UI 差异，公式可读，代码可复制。

> 💾 **Git 备份**：
```bash
git add frontend/src/components/AICard.vue frontend/src/components/ConversationPanel.vue
git commit -m "feat: 内容模块分流，数学公式渲染、代码复制"
git push
```

---

## 📦 任务包 6.6：历史课堂查看与整体打磨（预计 30 分钟）

**目标**：实现 `HistoryDetail.vue`，查看某个课堂的所有分析记录和对话历史，不可再编辑。

---

### ☑️ 1. 后端提供查询接口

在 `ClassroomController` 中增加：
```java
@GetMapping("/{classroomId}/records")
public ResponseEntity<?> getRecords(@PathVariable Long classroomId, Principal principal) {
    // 权限校验
    List<AnalysisRecord> records = recordRepo.findByClassroomIdOrderByCreatedAtDesc(classroomId);
    List<Map<String, Object>> result = records.stream().map(r -> {
        Map<String, Object> map = new HashMap<>();
        map.put("id", r.getId());
        map.put("whiteboardType", r.getWhiteboardType());
        map.put("aiResult", r.getAiResult());
        map.put("createdAt", r.getCreatedAt());
        return map;
    }).collect(Collectors.toList());
    return ResponseEntity.ok(result);
}

@GetMapping("/{classroomId}/messages")
public ResponseEntity<?> getMessages(@PathVariable Long classroomId, Principal principal) {
    List<ConversationMessage> msgs = msgRepo.findByClassroomIdOrderByTimestampAsc(classroomId);
    return ResponseEntity.ok(msgs);
}
```

---

### ☑️ 2. 前端 HistoryDetail.vue

```vue
<template>
  <div>
    <h1>{{ classroom.title }} - 历史记录</h1>
    <div v-for="record in records" :key="record.id">
      <AICard :content="record.aiResult" :type="record.whiteboardType" />
      <small>{{ record.createdAt }}</small>
    </div>
    <h2>对话记录</h2>
    <div v-for="msg in messages" :key="msg.id">
      <strong>{{ msg.role }}:</strong> <span v-html="msg.content"></span>
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { useRoute } from 'vue-router'
import { api } from '../services/api'
import { useClassroomStore } from '../stores/classroom'
import AICard from '../components/AICard.vue'

const route = useRoute()
const classroomId = route.params.classroomId
const records = ref([])
const messages = ref([])
const classroomStore = useClassroomStore()
const classroom = ref({})

onMounted(async () => {
  const res1 = await api.get(`/api/classrooms/${classroomId}/records`)
  records.value = res1.data
  const res2 = await api.get(`/api/classrooms/${classroomId}/messages`)
  messages.value = res2.data
})
</script>
```

---

### ☑️ 3. Dashboard 课堂列表链接

在 Dashboard 中，每个课堂增加“进入画板”和“查看历史”按钮。

---

### ☑️ 4. 最终全链路检查

- 注册登录 → 创建课堂 → 进入画板 → 写字发送 → AI 回答 → 追问 → 返回 Dashboard → 点击查看历史 → 看到所有记录和对话。
- 换个浏览器或清除 token，验证鉴权。

> ✅ **最终验收标准**：系统具备完整的用户体验闭环，所有数据持久化，多轮对话流畅，UI 卡片按类型区分。

> 💾 **Git 备份**：
```bash
git add .
git commit -m "feat: 第二阶段完成，业务闭环与交互层完工"
git push
```

---

## 🎯 第二阶段收尾

此刻，你们手中的系统不再是第一阶段的“正方体”技术 Demo，而是一个**有用户、有课堂、有对话、有内容分流的教学分析工具**。  
后续的“圆柱体”阶段可以继续打磨 UI、录制演示视频、编写文档等。

**立即行动**：请从 **任务包 6.1** 开始执行，有任何卡点随时沟通。祝大赛冲刺顺利！