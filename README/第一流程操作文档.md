遵循你的“正方体”阶段目标和任务全流程要求，我已将最小闭环拆解为 **5 个可直接执行的任务包**。每个包都包含具体命令、完整代码、验证方法和 Git 备份步骤。

---

## 🧱 第一阶段：正方体 —— 骨架搭建

### 📦 任务包 5.1：环境准备与依赖补齐（预计 15 分钟）

**目标**：确保前端、后端、AI 三个目录的依赖就绪，补全 AI 服务需要的 FastAPI 库。

> ☑️ 1. 打开终端，进入 `ai-service` 目录，更新 `requirements.txt`

在 `ai-service/requirements.txt` 末尾追加：

```text
fastapi==0.115.0
uvicorn[standard]==0.30.6
```

> ☑️ 2. 安装 Python 依赖

```bash
cd ai-service
pip install -r requirements.txt
```

> ☑️ 3. 安装前端依赖

```bash
cd ../frontend
npm install
```

> ☑️ 4. 确认后端可编译

```bash
cd ../backend
mvn clean compile
```

> ✅ **验收标准**：三条 `install/compile` 命令均无报错。

> 💾 **Git 备份**：
```bash
cd ~/Smart-Teaching-Board   # 回到项目根目录
git add ai-service/requirements.txt
git commit -m "chore: 补全 AI 服务依赖（FastAPI）"
git push
```

---

### 📦 任务包 5.2：构建 AI 微服务（预计 25 分钟）

**目标**：把已有的 `ai_analyzer.py` 包装成一个 HTTP API，后端可以直接调用。

**核心链路**：  
`POST /analyze` → 接收 `image_base64` → 调用 `ZhipuVisionAnalyzer.analyze_image()` → 返回 AIResponse JSON。

---

> ☑️ 1. 在 `ai-service/` 下创建 `server.py`

**完整代码**（直接复制全部）：

```python
# ai-service/server.py
import base64
import logging
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel, Field
from typing import Optional
from ai_analyzer import ZhipuVisionAnalyzer, WhiteboardType, AIResponse

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("AIServer")

app = FastAPI(title="云境智绘 AI 服务")

# 初始化分析器（从环境变量读取 API Key）
import os
API_KEY = os.environ.get("ZHIPU_API_KEY", "你的默认Key")
analyzer = ZhipuVisionAnalyzer(API_KEY)

class AnalyzeRequest(BaseModel):
    image_base64: str = Field(..., description="图片 Base64 编码（不含 data:image 前缀）")
    whiteboard_type: str = Field("general", description="内容类型：code, math, physics, general")
    context: Optional[str] = Field("", description="附加上下文，如课程名称")

class AnalyzeResponse(BaseModel):
    success: bool
    content: str
    error_msg: Optional[str] = None
    cost_time: float
    model_used: str

@app.post("/analyze", response_model=AnalyzeResponse)
async def analyze(request: AnalyzeRequest):
    try:
        # 类型转换
        type_map = {
            "code": WhiteboardType.CODE,
            "math": WhiteboardType.MATH,
            "physics": WhiteboardType.PHYSICS,
            "general": WhiteboardType.GENERAL
        }
        wb_type = type_map.get(request.whiteboard_type, WhiteboardType.GENERAL)
        response: AIResponse = analyzer.analyze_image(
            image_base64=request.image_base64,
            whiteboard_type=wb_type,
            context=request.context
        )
        return AnalyzeResponse(
            success=response.success,
            content=response.content,
            error_msg=response.error_msg,
            cost_time=response.cost_time,
            model_used=response.model_used
        )
    except Exception as e:
        logger.exception("AI 服务异常")
        raise HTTPException(status_code=500, detail=f"AI 服务内部错误：{str(e)}")

if __name__ == "__main__":
    import uvicorn
    uvicorn.run(app, host="0.0.0.0", port=8000)
```

**代码拆解说明**：
- 用 FastAPI 快速暴露 `/analyze` 端点。
- 接收的 JSON 字段与前端将要发送的一致：`image_base64`（不含前缀）、`whiteboard_type`、`context`。
- 内部复用你已有的 `ZhipuVisionAnalyzer`，不做重复开发。
- 把返回的 `AIResponse` 对象映射为 Pydantic 模型，自动序列化为 JSON。

---

> ☑️ 2. 在 `ai-service/` 目录下启动服务

```bash
cd ai-service
python server.py
```

看到 `Uvicorn running on http://0.0.0.0:8000` 表示启动成功。

> ☑️ 3. 用 curl 快速验证（新开终端）

```bash
curl -X POST http://localhost:8000/analyze \
  -H "Content-Type: application/json" \
  -d '{"image_base64":"'$(base64 -w 0 test.jpg)'","whiteboard_type":"math","context":"测试"}'
```

> ✅ **验收标准**：返回的 JSON 中包含 `success` 字段，且 `content` 或 `error_msg` 有合理值。

> 💾 **Git 备份**：
```bash
git add ai-service/server.py
git commit -m "feat: 添加 FastAPI 包装的 AI 微服务，提供 /analyze 端点"
git push
```

---

### 📦 任务包 5.3：后端中继接口开发（预计 35 分钟）

**目标**：在 Spring Boot 中增加一个 `/api/analyze` 接口，接收前端的截图请求，转发给 AI 微服务，并将结果原样返回。

**架构**：`前端 → POST /api/analyze → Spring Boot → POST http://localhost:8000/analyze → AI 微服务`

---

> ☑️ 1. 在 `backend/src/main/resources/application.yml` 增加 AI 服务地址

```yaml
server:
  port: 8080

ai:
  service:
    url: http://localhost:8000/analyze
```

> ☑️ 2. 创建 DTO 类：`backend/src/main/java/com/yourcompany/board/dto/AnalyzeRequest.java`

```java
package com.yourcompany.board.dto;

public class AnalyzeRequest {
    private String imageBase64;
    private String whiteboardType;
    private String context;

    // getters and setters
    public String getImageBase64() { return imageBase64; }
    public void setImageBase64(String imageBase64) { this.imageBase64 = imageBase64; }
    public String getWhiteboardType() { return whiteboardType; }
    public void setWhiteboardType(String whiteboardType) { this.whiteboardType = whiteboardType; }
    public String getContext() { return context; }
    public void setContext(String context) { this.context = context; }
}
```

> ☑️ 3. 创建 DTO 类：`backend/src/main/java/com/yourcompany/board/dto/AnalyzeResponse.java`

```java
package com.yourcompany.board.dto;

public class AnalyzeResponse {
    private boolean success;
    private String content;
    private String errorMsg;
    private double costTime;
    private String modelUsed;

    // getters and setters ...
}
```

> ☑️ 4. 创建 Controller：`backend/src/main/java/com/yourcompany/board/controller/AiController.java`

```java
package com.yourcompany.board.controller;

import com.yourcompany.board.dto.AnalyzeRequest;
import com.yourcompany.board.dto.AnalyzeResponse;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.http.*;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.client.RestTemplate;

@RestController
@RequestMapping("/api")
public class AiController {

    @Value("${ai.service.url}")
    private String aiServiceUrl;

    private final RestTemplate restTemplate = new RestTemplate();

    @PostMapping("/analyze")
    public ResponseEntity<AnalyzeResponse> analyze(@RequestBody AnalyzeRequest request) {
        try {
            // 转发给 AI 微服务
            HttpHeaders headers = new HttpHeaders();
            headers.setContentType(MediaType.APPLICATION_JSON);
            HttpEntity<AnalyzeRequest> entity = new HttpEntity<>(request, headers);

            ResponseEntity<AnalyzeResponse> aiResp = restTemplate.exchange(
                aiServiceUrl,
                HttpMethod.POST,
                entity,
                AnalyzeResponse.class
            );
            return ResponseEntity.ok(aiResp.getBody());
        } catch (Exception e) {
            AnalyzeResponse errorResp = new AnalyzeResponse();
            errorResp.setSuccess(false);
            errorResp.setErrorMsg("后端调用 AI 服务失败：" + e.getMessage());
            return ResponseEntity.status(500).body(errorResp);
        }
    }
}
```

> ☑️ 5. 启动后端

```bash
cd backend
mvn spring-boot:run
```

> ☑️ 6. 用 curl 验证后端接口（AI 微服务需已启动）

```bash
curl -X POST http://localhost:8080/api/analyze \
  -H "Content-Type: application/json" \
  -d '{"imageBase64":"'$(base64 -w 0 test.jpg)'","whiteboardType":"math","context":"测试"}'
```

> ✅ **验收标准**：返回的 JSON 结构与直接调 AI 服务完全一致，`success` 为 `true`，`content` 有 AI 识别结果。

> 💾 **Git 备份**：
```bash
git add backend/src/main/resources/application.yml
git add backend/src/main/java/com/yourcompany/board/dto/
git add backend/src/main/java/com/yourcompany/board/controller/AiController.java
git commit -m "feat: 添加 /api/analyze 中继接口，转发请求至 AI 微服务"
git push
```

---

### 📦 任务包 5.4：前端最小闭环页面（预计 50 分钟）

**目标**：在浏览器中完成“摄像头 → 手势写字 → 圈选 → 发送 AI → 渲染结果”的完整链路。

我们将利用 `frontend/public/` 放置一个独立的 HTML 文件，借助 Vite 的代理解决跨域。

---

> ☑️ 1. 在 `frontend/vite.config.ts` 中增加代理配置

```ts
import { defineConfig } from 'vite'

export default defineConfig({
  server: {
    proxy: {
      '/api': 'http://localhost:8080'
    }
  }
})
```

---

> ☑️ 2. 创建 `frontend/public/minimal-demo.html`

**完整代码**（直接粘贴，关键部分已注释）：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <title>云境智绘 - 最小闭环</title>
  <style>
    body { margin: 0; display: flex; font-family: sans-serif; }
    #left { width: 70%; position: relative; }
    #video, #outputCanvas { position: absolute; top: 0; left: 0; }
    #right { width: 30%; padding: 10px; border-left: 2px solid #ccc; background: #f9f9f9; overflow-y: auto; }
    #toolbar { margin-bottom: 10px; }
    button { margin-right: 5px; }
    #aiResult { background: white; padding: 10px; border-radius: 5px; min-height: 200px; }
  </style>
</head>
<body>
  <div id="left">
    <video id="video" autoplay playsinline style="width:100%; transform: scaleX(-1);"></video>
    <canvas id="outputCanvas" style="width:100%;"></canvas>
  </div>
  <div id="right">
    <div id="toolbar">
      <button id="clearBtn">清屏</button>
      <label>笔色：<input type="color" id="colorPicker" value="#000000"></label>
      <button id="sendBtn">圈选发送 AI</button>
    </div>
    <div id="selectionHint">按住 Shift 并拖动鼠标框选区域</div>
    <div id="aiResult"><em>等待 AI 结果...</em></div>
  </div>

  <!-- 引入 MediaPipe Hands 和 marked -->
  <script src="https://cdn.jsdelivr.net/npm/@mediapipe/camera_utils/camera_utils.js" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/@mediapipe/control_utils/control_utils.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@mediapipe/drawing_utils/drawing_utils.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@mediapipe/hands/hands.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>

  <script>
    const video = document.getElementById('video');
    const canvas = document.getElementById('outputCanvas');
    const ctx = canvas.getContext('2d');
    const aiResult = document.getElementById('aiResult');

    let isPinching = false;
    let lastX = 0, lastY = 0;
    let currentColor = '#000000';
    let selectionRect = null;      // { x, y, w, h }
    let isSelecting = false;
    let selectStart = { x: 0, y: 0 };

    // ---- 画布尺寸自适应 ----
    function resizeCanvas() {
      canvas.width = video.videoWidth || 640;
      canvas.height = video.videoHeight || 480;
    }

    // ---- 手势回调 ----
    function onResults(results) {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      if (results.multiHandLandmarks && results.multiHandLandmarks.length > 0) {
        const landmarks = results.multiHandLandmarks[0];
        // 食指指尖 (8), 拇指指尖 (4)
        const indexTip = landmarks[8];
        const thumbTip = landmarks[4];
        // 转换为画布坐标
        const ix = indexTip.x * canvas.width;
        const iy = indexTip.y * canvas.height;
        const tx = thumbTip.x * canvas.width;
        const ty = thumbTip.y * canvas.height;
        const distance = Math.hypot(ix - tx, iy - ty);

        // 捏合阈值（可根据实际调整）
        const pinchThreshold = 30;
        if (distance < pinchThreshold) {
          if (!isPinching) {
            isPinching = true;
            lastX = ix;
            lastY = iy;
          } else {
            // 画线
            ctx.beginPath();
            ctx.strokeStyle = currentColor;
            ctx.lineWidth = 4;
            ctx.lineCap = 'round';
            ctx.moveTo(lastX, lastY);
            ctx.lineTo(ix, iy);
            ctx.stroke();
            lastX = ix;
            lastY = iy;
          }
        } else {
          isPinching = false;
        }
      }
    }

    // ---- 初始化摄像头与MediaPipe ----
    async function initCamera() {
      const stream = await navigator.mediaDevices.getUserMedia({ video: true });
      video.srcObject = stream;
      await video.play();
      resizeCanvas();
      const hands = new Hands({locateFile: (file) => `https://cdn.jsdelivr.net/npm/@mediapipe/hands/${file}`});
      hands.setOptions({
        maxNumHands: 1,
        modelComplexity: 1,
        minDetectionConfidence: 0.7,
        minTrackingConfidence: 0.5
      });
      hands.onResults(onResults);
      const camera = new Camera(video, { onFrame: async () => { await hands.send({image: video}); } });
      camera.start();
    }

    // ---- 工具栏 ----
    document.getElementById('clearBtn').onclick = () => ctx.clearRect(0,0,canvas.width,canvas.height);
    document.getElementById('colorPicker').oninput = (e) => currentColor = e.target.value;

    // ---- 圈选功能（Shift+鼠标拖动） ----
    canvas.addEventListener('mousedown', (e) => {
      if (!e.shiftKey) return;
      isSelecting = true;
      const rect = canvas.getBoundingClientRect();
      const scaleX = canvas.width / rect.width;
      const scaleY = canvas.height / rect.height;
      selectStart = {
        x: (e.clientX - rect.left) * scaleX,
        y: (e.clientY - rect.top) * scaleY
      };
      selectionRect = null;
    });

    canvas.addEventListener('mousemove', (e) => {
      if (!isSelecting) return;
      const rect = canvas.getBoundingClientRect();
      const scaleX = canvas.width / rect.width;
      const scaleY = canvas.height / rect.height;
      const currentX = (e.clientX - rect.left) * scaleX;
      const currentY = (e.clientY - rect.top) * scaleY;
      selectionRect = {
        x: Math.min(selectStart.x, currentX),
        y: Math.min(selectStart.y, currentY),
        w: Math.abs(currentX - selectStart.x),
        h: Math.abs(currentY - selectStart.y)
      };
      // 重绘画布 + 圈选框
      ctx.save();
      // 这里简化，不清除原内容，仅画选择框示意
      // 为不干扰书写，在此我们只画半透明矩形提示
      ctx.strokeStyle = 'blue';
      ctx.lineWidth = 2;
      ctx.strokeRect(selectionRect.x, selectionRect.y, selectionRect.w, selectionRect.h);
      ctx.restore();
    });

    canvas.addEventListener('mouseup', () => {
      if (!isSelecting) return;
      isSelecting = false;
      // 再次重绘清除选择框（通过手势回调自动刷新，此处直接调一次 onResults 或简单清屏重绘）
      // 为简单起见，这里不做额外重绘，选择框会随着下一帧刷新消失
    });

    // ---- 发送给后端 ----
    document.getElementById('sendBtn').onclick = async () => {
      if (!selectionRect || selectionRect.w < 10 || selectionRect.h < 10) {
        alert('请先按住 Shift 键并拖动鼠标框选一个区域');
        return;
      }
      const cropCanvas = document.createElement('canvas');
      cropCanvas.width = selectionRect.w;
      cropCanvas.height = selectionRect.h;
      const cropCtx = cropCanvas.getContext('2d');
      cropCtx.drawImage(canvas, selectionRect.x, selectionRect.y, selectionRect.w, selectionRect.h, 0, 0, selectionRect.w, selectionRect.h);
      const base64 = cropCanvas.toDataURL('image/png').replace('data:image/png;base64,', '');

      aiResult.innerHTML = '<em>正在请求 AI...</em>';
      try {
        const resp = await fetch('/api/analyze', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            imageBase64: base64,
            whiteboardType: 'general',
            context: '测试课堂'
          })
        });
        const data = await resp.json();
        if (data.success) {
          aiResult.innerHTML = marked.parse(data.content);
        } else {
          aiResult.innerHTML = `<span style="color:red;">识别失败：${data.errorMsg || '未知错误'}</span>`;
        }
      } catch (err) {
        aiResult.innerHTML = `<span style="color:red;">网络错误：${err.message}</span>`;
      }
    };

    // 启动
    initCamera();
  </script>
</body>
</html>
```

**代码说明**：
- 使用 MediaPipe Hands 检测手势，计算食指与拇指距离实现“捏合书写”。
- 按住 Shift 并拖动鼠标框选区域，松开后点击“圈选发送 AI”将裁剪区域转 Base64。
- 通过 Fetch 调用 `/api/analyze`（Vite 代理到 `localhost:8080`）。
- 返回的 Markdown 内容用 marked 库渲染到右侧面板。

---

> ☑️ 3. 启动前端开发服务器

```bash
cd frontend
npm run dev
```

> ☑️ 4. 浏览器访问 `http://localhost:5173/minimal-demo.html`

> ✅ **验收标准**：
> - 允许摄像头权限后，能看到镜像视频。
> - 食指与拇指捏合可在 Canvas 上画出线条。
> - 按住 Shift 框选一块区域，点击按钮后右侧出现 AI 解析的 Markdown 结果（若无 AI Key 则显示错误提示，但整个链路已通）。

> 💾 **Git 备份**：
```bash
git add frontend/vite.config.ts
git add frontend/public/minimal-demo.html
git commit -m "feat: 添加最小闭环前端页面，实现手势写字+圈选发送AI"
git push
```

---

### 📦 任务包 5.5：全链路联调与异常处理（预计 20 分钟）

**目标**：验证整个“正方体”在本地无致命错误，并优雅处理 AI 失败情况。

> ☑️ 1. 确保三个进程都在运行：
- AI 微服务：`http://localhost:8000`（`python server.py`）
- 后端：`http://localhost:8080`
- 前端：`http://localhost:5173`

> ☑️ 2. 在浏览器中打开 `minimal-demo.html`，完整走通一遍：
1. 用捏合手势写几个字或画个图形。
2. 按住 Shift 框选它，点击“圈选发送 AI”。
3. 观察右侧是否出现 AI 返回的 Markdown 内容。

> ☑️ 3. 模拟 AI 服务异常：  
> 将 AI 微服务关闭，再次点击发送，后端应返回 500 并携带错误信息，前端显示 “识别失败：后端调用 AI 服务失败...” 的提示。

> ☑️ 4. 检查浏览器控制台（F12）的网络请求：
- `/api/analyze` 状态码应为 200（成功时）或 500（异常时）。
- 请求 Payload 包含 `imageBase64`、`whiteboardType`、`context`。

> ✅ **最终验收标准**：
> - Canvas 截图 → Base64 → HTTP → AI → Markdown 渲染 全部无致命错误。
> - AI 不可用时前端能显示友好的错误提示，不出现白屏或未捕获异常。

> 💾 **Git 备份**：
```bash
git add .
git commit -m "feat: 完成正方体阶段全链路联调，最小闭环可运行"
git push
```

---

### 🧊 正方体阶段总结

此刻你拥有一个**粗糙但完整的石膏块**：  
`摄像头 → 手势识别 → 画板写字 → 框选截图 → 发送后端 → 调用 AI → 返回识别结果 → Markdown 渲染展示`

后续的“圆柱体”阶段可以在此基础上增加登录、历史记录、多类型 Prompt、UI 美化等。  
至此，第一阶段目标达成，代码已全部提交到远程仓库。